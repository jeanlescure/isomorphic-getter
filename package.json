{
  "name": "isomorphic-getter",
  "version": "1.0.0",
  "description": "Isomorphic replacement for `require()`, for Webpack and React environments",
  "main": "lib/index",
  "scripts": {
    "build": "babel modules -d lib",
    "test": "npm run build && jasmine JASMINE_CONFIG_PATH=spec/jasmine.json"
  },
  "author": "Jean M. Lescure",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.7.7",
    "babel-core": "^6.7.7",
    "babel-preset-es2015": "^6.0.15",
    "babel-preset-stage-0": "^6.0.15",
    "jasmine": "^2.4.1"
  }
}
